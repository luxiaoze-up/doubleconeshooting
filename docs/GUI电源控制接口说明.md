# GUI 电源控制接口说明文档

**版本**: 1.0  
**日期**: 2024-12-23  
**适用设备**: 六自由度、大行程、反射光成像、辅助支撑

---

## 一、功能说明

### 1.1 自动模式（默认）

设备启动时**自动执行**电源控制：

```
设备启动 → 连接控制器 → 驱动器上电 → 刹车释放（如有）→ 就绪
```

**无需GUI操作**，系统自动完成初始化。

### 1.2 手动模式（GUI控制）

GUI可以通过Tango命令和属性实现手动控制和状态监控。

---

## 二、配置方式

### 2.1 配置文件自动生效

电源控制参数在 `config/devices_config.json` 中配置，设备启动时自动读取并应用：

```json
{
  "six_dof_1": {
    "props": {
      "driverPowerPort": "0",           // 驱动器上电端口
      "driverPowerController": "sys/motion/1",  // 控制器名称
      "brakePowerPort": "3",            // 刹车供电端口
      "brakePowerController": "sys/motion/1"
    }
  }
}
```

**配置生效流程**:
1. ✅ 启动设备 → 读取配置
2. ✅ 连接运动控制器 → 自动上电
3. ✅ 释放刹车（如有）→ 设备就绪

**无需额外操作！**

---

## 三、Tango 接口定义

### 3.1 六自由度设备 (sys/six_dof/1)

#### 命令 (Commands)

| 命令名 | 输入 | 输出 | 功能 |
|--------|------|------|------|
| `enableDriverPower` | 无 | 无 | 手动启动驱动器电源 |
| `disableDriverPower` | 无 | 无 | 手动关闭驱动器电源 |
| `releaseBrake` | 无 | 无 | 手动释放刹车 |
| `engageBrake` | 无 | 无 | 手动启用刹车 |
| `queryPowerStatus` | 无 | JSON字符串 | 查询电源状态 |

#### 属性 (Attributes)

| 属性名 | 类型 | 读写 | 说明 |
|--------|------|------|------|
| `driverPowerStatus` | BOOLEAN | READ | 驱动器电源状态 (true=已上电) |
| `brakeStatus` | BOOLEAN | READ | 刹车状态 (true=已释放) |

#### queryPowerStatus 返回格式

```json
{
  "driverPowerEnabled": true,
  "brakeReleased": true,
  "driverPowerPort": 0,
  "brakePowerPort": 3,
  "driverPowerController": "sys/motion/1",
  "brakePowerController": "sys/motion/1"
}
```

---

### 3.2 大行程设备 (sys/large_stroke/1)

**接口与六自由度相同**，包含驱动器和刹车控制。

配置参数：
```json
"driverPowerPort": "0",  // 与六自由度共用OUT0
"brakePowerPort": "4"    // 独立刹车端口OUT4
```

---

### 3.3 反射光成像设备 (sys/reflection/1)

#### 命令 (Commands)

| 命令名 | 功能 |
|--------|------|
| `enableDriverPower` | 手动启动驱动器电源 |
| `disableDriverPower` | 手动关闭驱动器电源 |
| `queryPowerStatus` | 查询电源状态 |

#### 属性 (Attributes)

| 属性名 | 类型 | 说明 |
|--------|------|------|
| `driverPowerStatus` | BOOLEAN | 驱动器电源状态 |

**注意**: 该设备无刹车，不提供刹车控制接口。

---

### 3.4 辅助支撑设备 (sys/auxiliary/1-5)

**接口与反射光成像相同**，仅驱动器控制，无刹车。

配置参数：
```json
"driverPowerPort": "6",  // 5个设备共用OUT6
"driverPowerController": "sys/motion/1"
```

---

## 四、GUI 实现建议

### 4.1 状态监控面板

#### 六自由度设备示例

```
┌─────────────────────────────────────┐
│ 六自由度设备 - 电源状态              │
├─────────────────────────────────────┤
│ 驱动器电源:  ● 已上电               │
│ 刹车状态:    ● 已释放               │
│                                     │
│ [关闭驱动器] [启用刹车] [刷新状态]  │
└─────────────────────────────────────┘
```

#### 实现代码（Python/PyTango）

```python
import tango

# 连接设备
device = tango.DeviceProxy("sys/six_dof/1")

# 读取状态
driver_power = device.read_attribute("driverPowerStatus").value
brake_released = device.read_attribute("brakeStatus").value

# 显示状态
print(f"驱动器电源: {'已上电' if driver_power else '已断电'}")
print(f"刹车状态: {'已释放' if brake_released else '已锁定'}")

# 查询详细状态
status_json = device.command_inout("queryPowerStatus")
print(f"详细状态: {status_json}")
```

---

### 4.2 控制按钮

#### 按钮定义

| 按钮 | 命令 | 确认提示 |
|------|------|---------|
| **启动驱动器** | `enableDriverPower` | "确认启动驱动器电源？" |
| **关闭驱动器** | `disableDriverPower` | "⚠️ 确认关闭驱动器？电机将断电！" |
| **释放刹车** | `releaseBrake` | "确认释放刹车？" |
| **启用刹车** | `engageBrake` | "确认启用刹车？电机将锁定！" |

#### 实现代码（Python/PyTango）

```python
def enable_driver_power():
    try:
        device.command_inout("enableDriverPower")
        print("✓ 驱动器电源已启动")
    except tango.DevFailed as e:
        print(f"✗ 启动失败: {e.args[0].desc}")

def release_brake():
    try:
        device.command_inout("releaseBrake")
        print("✓ 刹车已释放")
    except tango.DevFailed as e:
        print(f"✗ 释放失败: {e.args[0].desc}")
```

---

### 4.3 状态指示灯

#### 颜色定义

| 状态 | 颜色 | 说明 |
|------|------|------|
| 驱动器已上电 | 🟢 绿色 | 正常工作状态 |
| 驱动器已断电 | 🔴 红色 | 断电状态 |
| 刹车已释放 | 🟢 绿色 | 可以运动 |
| 刹车已锁定 | 🟡 黄色 | 电机锁定 |
| 未配置 | ⚪ 灰色 | 功能未启用 |

---

### 4.4 自动刷新

建议GUI每2-5秒自动刷新状态：

```python
import time
from threading import Thread

def auto_refresh_status(device_proxy, interval=3):
    """自动刷新设备状态"""
    while True:
        try:
            driver_power = device_proxy.read_attribute("driverPowerStatus").value
            brake_status = device_proxy.read_attribute("brakeStatus").value
            # 更新GUI显示
            update_gui(driver_power, brake_status)
        except Exception as e:
            print(f"刷新失败: {e}")
        time.sleep(interval)

# 启动后台刷新线程
refresh_thread = Thread(target=auto_refresh_status, args=(device, 3))
refresh_thread.daemon = True
refresh_thread.start()
```

---

## 五、GUI 布局建议

### 5.1 设备控制面板

```
┌──────────────────────────────────────────────────────┐
│ 设备控制面板                                          │
├──────────────────────────────────────────────────────┤
│                                                      │
│  六自由度设备 (sys/six_dof/1)                        │
│  ┌────────────────────────────────────────────────┐ │
│  │ 驱动器电源: 🟢 已上电   [关闭驱动器]           │ │
│  │ 刹车状态:   🟢 已释放   [启用刹车]             │ │
│  │ 控制器: 192.168.1.11 OUT0/OUT3                 │ │
│  └────────────────────────────────────────────────┘ │
│                                                      │
│  大行程设备 (sys/large_stroke/1)                     │
│  ┌────────────────────────────────────────────────┐ │
│  │ 驱动器电源: 🟢 已上电   [关闭驱动器]           │ │
│  │ 刹车状态:   🟢 已释放   [启用刹车]             │ │
│  │ 控制器: 192.168.1.11 OUT0/OUT4                 │ │
│  └────────────────────────────────────────────────┘ │
│                                                      │
│  反射光成像 (sys/reflection/1)                       │
│  ┌────────────────────────────────────────────────┐ │
│  │ 驱动器电源: 🟢 已上电   [关闭驱动器]           │ │
│  │ 控制器: 192.168.1.11 OUT5                      │ │
│  └────────────────────────────────────────────────┘ │
│                                                      │
│  辅助支撑 (sys/auxiliary/1-5)                        │
│  ┌────────────────────────────────────────────────┐ │
│  │ 驱动器电源: 🟢 已上电   [关闭驱动器]           │ │
│  │ 控制器: 192.168.1.11 OUT6 (5设备共用)          │ │
│  └────────────────────────────────────────────────┘ │
│                                                      │
│  [全部启动] [全部关闭] [刷新状态] [查看日志]        │
└──────────────────────────────────────────────────────┘
```

---

### 5.2 批量操作

```python
def enable_all_devices():
    """启动所有设备的驱动器电源"""
    devices = [
        "sys/six_dof/1",
        "sys/large_stroke/1",
        "sys/reflection/1",
        "sys/auxiliary/1",
        "sys/auxiliary/2",
        "sys/auxiliary/3",
        "sys/auxiliary/4",
        "sys/auxiliary/5"
    ]
    
    for dev_name in devices:
        try:
            device = tango.DeviceProxy(dev_name)
            device.command_inout("enableDriverPower")
            print(f"✓ {dev_name} 驱动器已启动")
        except Exception as e:
            print(f"✗ {dev_name} 启动失败: {e}")

def release_all_brakes():
    """释放所有带刹车设备的刹车"""
    brake_devices = ["sys/six_dof/1", "sys/large_stroke/1"]
    
    for dev_name in brake_devices:
        try:
            device = tango.DeviceProxy(dev_name)
            device.command_inout("releaseBrake")
            print(f"✓ {dev_name} 刹车已释放")
        except Exception as e:
            print(f"✗ {dev_name} 释放失败: {e}")
```

---

## 六、安全建议

### 6.1 操作权限

建议在GUI中实现权限控制：

| 操作 | 权限等级 | 说明 |
|------|---------|------|
| 查看状态 | 所有用户 | 只读操作 |
| 启动驱动器 | 操作员 | 需要确认 |
| 关闭驱动器 | 管理员 | 需要二次确认 |
| 刹车控制 | 操作员 | 需要确认 |

### 6.2 操作确认

关键操作需要确认对话框：

```python
def disable_driver_power_with_confirm():
    """带确认的驱动器断电"""
    response = messagebox.askyesno(
        "警告",
        "⚠️ 确认关闭驱动器电源？\n\n"
        "这将导致：\n"
        "- 电机断电\n"
        "- 无法运动\n"
        "- 需要重新启动才能恢复\n\n"
        "是否继续？"
    )
    
    if response:
        device.command_inout("disableDriverPower")
```

### 6.3 状态检查

运动前自动检查电源状态：

```python
def check_power_before_motion(device_proxy):
    """运动前检查电源状态"""
    driver_power = device_proxy.read_attribute("driverPowerStatus").value
    
    if not driver_power:
        raise Exception("驱动器电源未启动，无法运动！")
    
    # 检查刹车（如果有）
    try:
        brake_released = device_proxy.read_attribute("brakeStatus").value
        if not brake_released:
            raise Exception("刹车未释放，无法运动！")
    except:
        pass  # 设备可能没有刹车
    
    return True
```

---

## 七、故障排查

### 7.1 常见问题

| 问题 | 原因 | 解决方法 |
|------|------|---------|
| 驱动器无法启动 | 控制器连接失败 | 检查控制器IP和连接 |
| 刹车无法释放 | OUT端口故障 | 检查硬件接线 |
| 状态显示错误 | 属性读取失败 | 重启设备 |
| 命令执行失败 | 权限不足 | 检查用户权限 |

### 7.2 日志查看

```python
# 查看设备日志
device = tango.DeviceProxy("sys/six_dof/1")
logs = device.read_attribute("sixLogs").value
print(logs)

# 查询电源状态历史
status = device.command_inout("queryPowerStatus")
print(f"当前状态: {status}")
```

---

## 八、总结

### 配置生效方式

✅ **自动模式（推荐）**: 配置文件 → 设备启动 → 自动上电/释放刹车  
✅ **手动模式（可选）**: GUI通过Tango命令手动控制

### GUI需要实现的功能

1. ✅ **状态监控**: 读取 `driverPowerStatus` 和 `brakeStatus` 属性
2. ✅ **手动控制**: 调用 `enableDriverPower`、`releaseBrake` 等命令
3. ✅ **批量操作**: 一键启动/关闭所有设备
4. ✅ **安全确认**: 关键操作需要用户确认
5. ✅ **实时刷新**: 自动更新状态显示

### 开发优先级

1. **高优先级**: 状态监控（只读）
2. **中优先级**: 手动控制按钮
3. **低优先级**: 批量操作、权限管理

---

**文档维护**: 如有接口变更，请及时更新本文档。  
**技术支持**: 系统集成团队

